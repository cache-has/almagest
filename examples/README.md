# Almagest examples

Sample `.alm` files demonstrating different use cases (solo-founder dashboards,
consultant deliverables, embedded analytics, offline field work).

**Status:** populated once the format (Phase 02) and DSL (Phase 05) land. The
Orrery example dashboards (`../../openboard/examples/dashboards/*.board`) plus the
pagila seed are the seed corpus — transpiled into Almagest's JSON dashboard format
to double as demos and regression fixtures.
