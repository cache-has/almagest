# Almagest documentation

User-facing docs: quick start, the `.alm` format spec, the dashboard DSL, the
embedding guide, and the CLI reference.

**Status:** written alongside the features they document. The format spec
(Phase 02) and DSL spec (Phase 05) are the first to land here.
